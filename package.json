{
  "name": "gvpcew-app",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "db:init": "npm --prefix backend run db:init"
  }
}
