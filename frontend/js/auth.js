// Login, registration, and face-enrollment entry point.
// The implementation remains in the React source tree so it can share the
// existing API client and UI components with the role dashboards.
import '../src/main.jsx';
